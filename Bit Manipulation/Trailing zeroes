https://www.interviewbit.com/problems/trailing-zeroes/

int Solution::solve(int A) {

    int count=0;

    while(A>0)
    {
        if(A%2==0)
        {
            count++;
            A=A/2;
        }

        else
            break;
    }

    return count;
}
